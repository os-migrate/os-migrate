name: Python Lint

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    name: Python Lint
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Run Ruff Lint and Format check
        uses: astral-sh/ruff-action@v3
        with:
          # Pin the Ruff version so its default rule set cannot silently change
          # between releases and break CI without a code change.
          version: "0.16.8"
          # This runs both the linter and format verification
          args: "check --output-format=github ."
