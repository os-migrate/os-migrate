- name: Conversion host content tasks
  block:
    - name: Display host os
      ansible.builtin.debug:
        msg: >-
          Conversion Host OS is
          {{ ansible_distribution }} {{ ansible_distribution_version }}

    - name: Running pre content hook
      ansible.builtin.shell: "{{ conversion_host_content_pre_content_hook }}"
      args:
        executable: /bin/bash
      when: conversion_host_content_pre_content_hook is string

    - name: Include CentOS tasks
      ansible.builtin.include_tasks: centos.yml
      when: ansible_distribution in ['CentOS']

    - name: Include RHEL tasks
      ansible.builtin.include_tasks: rhel.yml
      when: ansible_distribution in ['RedHat']

    - name: Running post content hook
      ansible.builtin.shell: "{{ conversion_host_content_post_content_hook }}"
      args:
        executable: /bin/bash
      when: conversion_host_content_post_content_hook is string
