---
galaxy_info:
  authors:
    - "os-migrate (@os-migrate) team"
  role_name: {{ role_name }}
  namespace: os_migrate
  description: ### CHANGEME ###
  company: Red Hat
  license: Apache-2.0
  min_ansible_version: 2.18
  galaxy_tags:
    - "osmigrate"
{% if 'export' in role_name %}
dependencies:
  - role: os_migrate.os_migrate.prelude_src
{% elif 'import' in role_name %}
dependencies:
  - role: os_migrate.os_migrate.prelude_dst
{% else %}
dependencies: []
{% endif %}
