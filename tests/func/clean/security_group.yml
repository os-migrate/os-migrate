---
- name: remove osm_security_group
  os_security_group:
    cloud: "{{ item }}"
    name: osm_security_group
    state: absent
  loop:
    - "{{ os_migrate_src_cloud }}"
    - "{{ os_migrate_dst_cloud }}"
