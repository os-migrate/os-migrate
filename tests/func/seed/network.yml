---
# Fails with: TypeError: create_network()
# got an unexpected keyword argument 'dns_domain'
#
# - name: create osm_net
#   os_network:
#     cloud: "{{ os_migrate_src_cloud }}"
#     name: osm_net
#     description: osm_net test network
#     state: present
- name: workaround - create osm_net via CLI
  changed_when: true
  shell: |
    openstack network create \
        --os-cloud "{{ os_migrate_src_cloud }}" \
        --description "osm_net test network" \
        osm_net
