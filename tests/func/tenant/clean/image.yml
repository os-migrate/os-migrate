-   name: remove osm_image
    loop:
    -   "{{ os_migrate_src_auth }}"
    -   "{{ os_migrate_dst_auth }}"
    ansible.builtin.os_image:
      auth: "{{ item }}"
      name: osm_image
      state: absent
