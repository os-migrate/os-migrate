-   name: remove osm_security_group
    loop:
    -   "{{ os_migrate_src_auth }}"
    -   "{{ os_migrate_dst_auth }}"
    ansible.builtin.os_security_group:
      auth: "{{ item }}"
      name: osm_security_group
      state: absent
